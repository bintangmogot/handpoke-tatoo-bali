# Sitemap & Flow

## Public Routes
1. `/` (Overlay) -> "Enter Studio" unlocks music
2. `/` (Home) -> Hero, Teasers, Reviews, CTA
3. `/about` -> Story, Philosophy, Roots
4. `/services` -> Session Pricing, Flash Tattoos, Process
5. `/gallery` -> Filterable Handpoke/Machine/Flash grids
6. `/faq` -> Medical limits, minimum sizes, aftercare
7. `/blog` -> SEO Article Listing
   - `/blog/[slug]` -> Individual Articles
8. `/booking` -> Flash Booking Flow & Custom Booking Gateway

## Admin Routes (Protected)
1. `/admin` -> Dashboard Calendar, Reviews Manager, Invoice Generator
